1. In non-cloud environments where we can not use a cloud environment load balancer for load balancer kind service Metal LB can be used to provision the load balancer service.
2. Metallb Use the BGP and ARP to work.

Steps:-

1. First install the Metal LB load balancer operator from Operator Hub.

2. 
